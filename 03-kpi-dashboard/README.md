# Automated Operations Reporting & KPI Dashboard

**Live dashboard:** https://miraokonkwo.github.io/data-analytics-portfolio/weekly-ops-report/

This one is the most direct match to my actual job. In my role as a Data and Operations Coordinator, I track scheduling, workflow, and performance data and write it up into a weekly report for management. This project takes a raw call center log and automates exactly that: the same KPIs, the same weekly cadence, generated by a script instead of rebuilt by hand in Excel every Monday.

## What's in the report

- **Volume:** calls received per week
- **Abandonment rate:** the share of calls that hang up before reaching an agent
- **First-contact resolution (FCR):** the share of answered calls resolved without a callback
- **Average handle time:** how long an agent spends on a typical call
- **Customer satisfaction (CSAT):** the average post-call rating
- All of the above broken down by department and by agent, plus week-over-week deltas on the headline KPI tiles

## What I found

1. **Call volume is stable.** It ranges from 356 to 442 calls a week across the quarter, with no clear upward or downward trend, meaning it's predictable enough to staff against.
2. **Abandonment rate is the real problem.** It never dropped below 16% in any of the 12 full weeks in the quarter. A typical call center benchmark is 5 to 8%. This queue ran at double to quadruple that every single week, not just on a bad day.
3. **Department and agent performance are both fairly even.** FCR sits in the high 80s to low 90s across all five departments and all eight agents, and CSAT hovers around 3.3 to 3.5 out of 5 everywhere. The abandonment problem isn't a single team or product line, it's systemic.

## Why this matters as a portfolio piece

A one-off report can miss a problem like the abandonment rate because a single bad week looks like an anomaly. A running weekly report catches it immediately, because week 1 and week 12 tell the same story. That's the actual value of automating a report like this: it turns "did this get worse" into a question you can answer by looking, not by re-pulling data.

## Data and methods

- **Source:** a public call center call log (`data/call_center_raw.csv`), 5,000 calls across 9 agents and 5 product departments, January to March 2015. See [Data credit](#data-credit) below.
- **Cleaning:** one call was logged under agent "L", a single-letter name with only 1 call in the whole dataset. I treated it as a data-entry artifact and excluded it from the agent leaderboard rather than reporting on a "ninth agent" with a sample size of one.
- **Analysis:** Python and pandas, in [`notebooks/01_analysis.py`](notebooks/01_analysis.py). It computes weekly, department-level, and agent-level KPIs from the raw call log and writes them to `outputs/`.
- **Partial weeks:** the first and last calendar weeks in the data only cover part of a week (the data starts January 1 and ends March 31, neither of which lands on a Monday), so I excluded them from the trend charts to avoid a misleading dip or spike at either edge.
- **Dashboard:** a single-page report (`dashboard.html`) that reads like the actual weekly report I'd hand to a manager: headline KPI tiles with week-over-week deltas, two trend charts, and department and agent breakdown tables.

## The honest limitation

This is a static HTML page built from a CSV snapshot, not a live-connected Power BI or Tableau report sitting on top of a real database. The KPI logic in `01_analysis.py` is exactly what I'd port into Power BI's data model or a scheduled Python pipeline if this were plugged into a live system. I built it this way so the dashboard is viewable by anyone with a link, with nothing to install.

## Repo structure

```
03-kpi-dashboard/
├── README.md
├── dashboard.html              # the published weekly report
├── data/
│   └── call_center_raw.csv
├── notebooks/
│   └── 01_analysis.py
└── outputs/
    ├── weekly_kpis.csv
    ├── department_kpis.csv
    ├── agent_kpis.csv
    └── satisfaction_distribution.csv
```

## Data credit

Call log CSV from [piyusharya8700/CALL-CENTER-DATASET](https://github.com/piyusharya8700/CALL-CENTER-DATASET) on GitHub, a cleaned version of a commonly used public call center practice dataset.
